<Project Sdk="Godot.NET.Sdk/4.7.2">

  <PropertyGroup>
    <TargetFramework>net8.0</TargetFramework>
    <!-- Godot 4.7's Android export template runs .NET 9, so phones get net9.0 (it needs the .NET 9 SDK). -->
    <TargetFramework Condition=" '$(GodotTargetPlatform)' == 'android' ">net9.0</TargetFramework>
    <EnableDynamicLoading>true</EnableDynamicLoading>
    <RootNamespace>TuTienLuc</RootNamespace>
    <Nullable>enable</Nullable>
    <LangVersion>12</LangVersion>
  </PropertyGroup>

  <ItemGroup>
    <!-- The rules live in the engine-free core library (see design/GAME_DESIGN.md §9.2). -->
    <ProjectReference Include="../core/TuTien.Core/TuTien.Core.csproj" />
  </ItemGroup>

</Project>
